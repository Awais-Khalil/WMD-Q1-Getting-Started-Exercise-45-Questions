const famousPerson1 = 'Albert Einstein';
const famousQuote1 = 'A person who never made a mistake never tried anything new.';
const message = `${famousPerson1} once said, ${famousQuote1}`;

console.log(message);
