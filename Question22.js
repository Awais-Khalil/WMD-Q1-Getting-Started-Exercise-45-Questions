"use strict";
const myArray = [1, 2, 3, 4, 5];
const index = 10; // set index to a value that is out of range
console.log(myArray[index]); // accessing an out-of-range index will produce an error
const index1 = 4;
console.log(myArray[index1]);
